# Simulation Runners

Scripts that run full scenario episodes.

Suggested commands later:

```bash
python run_scene.py --scene park_patrol --policy mock_policy_v0
python run_scene.py --scene disaster_response --policy mock_policy_v0
```
