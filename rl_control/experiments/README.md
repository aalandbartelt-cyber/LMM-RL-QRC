# RL Experiments

Each experiment should be saved as:

```text
experiments/exp_001/
  config.yaml
  train.log
  curve.png
  README.md
```

Do not commit large checkpoints directly. Put download instructions or storage links in the experiment README.
