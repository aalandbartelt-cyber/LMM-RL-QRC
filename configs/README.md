# Configs

Shared configuration files for scenes, policies, task schemas, and evaluation.

Recommended files:

- `scenes.yaml`: scene names, start points, waypoints, target objects.
- `policy.yaml`: selected locomotion policy and safety limits.
- `evaluation.yaml`: number of episodes, random seeds, terrain difficulty.
- `robot.yaml`: robot model, control frequency, observation fields.
